package io.github.khezyapp.dhttp.spec;

/**
 * Supported HTTP request methods for a {@link RequestShape}.
 */
public enum HttpMethod {
    GET,
    POST,
    PUT,
    PATCH,
    DELETE,
    HEAD,
    OPTIONS
}
